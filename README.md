# sequelize-pg-stream

_Make model classes streamable_

## Getting Started

To install:

    npm i --save sequelize-pg-stream

In your project:

``` javascript
 // => Example
```

## npm scripts

   * `tes`
   * `test:unit`
   * `test:int`
   * `cove`
   * `cover:unit`
   * `lint`
   * `serve:coverage`
   * `serve:linter`
